[phases.setup]
nixPkgs = ['...', 'nodejs', 'npm']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader --classmap-authoritative']

[phases.build]
cmds = [
    'npm ci',
    'npm run build',
    'node generate-icons.cjs'
]

[start]
cmd = 'bash start.sh'
